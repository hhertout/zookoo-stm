# Benchmark files

To ensure the good performance of Zookoo, we include benchmark files that help measure and analyze the system's efficiency under various conditions. These benchmarks are crucial for identifying bottlenecks and optimizing performance.

The benchmark files are located in the `benchmark/` directory of the project. They include various test scenarios that simulate real-world usage patterns.